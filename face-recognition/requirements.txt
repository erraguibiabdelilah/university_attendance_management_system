# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # required for Form + File uploads

# ── Settings ─────────────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Computer Vision & Face Recognition ───────────────────────────────────────
deepface==0.0.93               # ArcFace, FaceNet, DeepFace models
opencv-python==4.9.0.80        # image I/O, preprocessing, webcam
numpy==1.26.4

# DeepFace pulls these in, but pin for reproducibility
tensorflow==2.16.1             # DeepFace backend
tf-keras==2.16.0
retina-face==0.0.17            # RetinaFace detector
mtcnn==0.1.1                   # MTCNN detector (fallback)

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow==10.3.0                  # additional image format support
requests==2.32.3                # used internally by DeepFace for model downloads
tqdm==4.66.4
